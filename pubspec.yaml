name: beyonic_flutter
description: Flutter package to use Beyonic APIs
version: 0.1.1
homepage: https://apidocs.beyonic.com

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
